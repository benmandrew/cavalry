{ n >= 0 }
a <- array(n);
i <- 0;
while i < len(a) do
  invariant { 0 <= i && i <= len(a) && forall j . 0 <= j && j < i -> a[j] = 5 }
  variant { len(a) - i }
  a[i] <- 5;
  i <- i + 1
end
{ forall j . 0 <= j && j < len(a) -> a[j] = 5 }
